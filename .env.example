# MongoDB connection — must match docker-compose.yml credentials
MONGO_URI=mongodb://admin:changeme@localhost:27017/?authSource=admin
MONGO_DB=systematic_review

# Docker Compose uses these to initialize the MongoDB container
MONGOUSER=admin
MONGOPASSWORD=changeme
